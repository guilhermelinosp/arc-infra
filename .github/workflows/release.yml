name: release

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    uses: guilhermelinosp/ci-templates/.github/workflows/release-please.yml@main
    secrets: inherit
